Multimodalidade é a fronteira da IA: processar **vídeo, áudio e texto** juntos. Cada modalidade tem suas técnicas e modelos específicos — mas o fluxo de trabalho é sempre o mesmo: **transformar dados brutos em informação estruturada que uma aplicação pode consumir**.

## Texto

- **Classificação de tópicos**: categorizar documentos (spaCy, BERT, TF-IDF + classifier)
- **Sumarização**: extrair o essencial (extractive ou abstractive)
- **Análise de sentimentos**: positivo/negativo/neutro

## Hands-on — classificação de texto

<SandboxRunner lessonSlug="analise-video-audio" />

## Exercício 1: classificando reviews

O fluxo completo de análise de texto em produção: **vetorizar (TF-IDF) → treinar → classificar texto novo**. Rode e veja o modelo classificar reviews que ele nunca viu:

<Exercise
  lessonSlug="analise-video-audio"
  codeKey="videoaudio-ex1"
  title="Exercício 1 — Classificação de reviews"
  expectedOutput="'pessimo, nao funciona' -> negativo"
  hint="O modelo aprendeu com 4 exemplos e generaliza para 2 novos. Repare: 'excelente' e 'quebrado' foram as pistas decisivas. Em produção você teria milhares de exemplos — o padrão é idêntico."
/>

**Pergunta-chave:** por que o modelo acertou o segundo review sem nunca ter visto a palavra "pessimo" exata? (Resposta: o TF-IDF criou um vocabulário onde palavras de reviews negativos ficaram associadas ao label 0. A combinação de tokens — não a palavra exata — define a classe.)

## Exercício 2: o pipeline áudio → texto → sentimento

Transcrição (Whisper) gera texto; a análise de sentimento roda em cima. Simule o pipeline com palavras-chave — o mesmo padrão que um modelo faria, sem treino:

<Exercise
  lessonSlug="analise-video-audio"
  codeKey="videoaudio-ex2"
  title="Exercício 2 — Pipeline de transcrição"
  expectedOutput="mas o servidor caiu de novo e os cl... -> negativo"
  hint="A segunda fala tem 2 palavras negativas ('caiu', 'reclamaram') e 0 positivas → negativo. É uma heurística simples; um modelo de sentimento treinado faria o mesmo com mais nuance."
/>

**O que observar:** o pipeline multimodal real é **sequencial** — cada etapa consome a saída da anterior (áudio→texto→sentimento). É a mesma arquitetura da chain da Fase 03, aplicada a mídia.

## Áudio

- **Transcrição**: fala → texto (Whisper, Vosk, Deepgram)
- **Clasificação de som**: identificar eventos sonoros
- **Diarização**: quem falou quando

## Vídeo

- **Reconhecimento facial**: identificar pessoas e expressões
- **Detecção de atividade**: ações em vídeo (caminhar, correr, cair)
- **OCR em vídeo**: texto em cenas
- **Frame analysis**: processar quadros-chave com visão computacional

## Modelos principais

| Modalidade | Modelos |
|---|---|
| Texto | BERT, GPT, spaCy, NLTK |
| Áudio | Whisper, Wav2Vec2, Vosk |
| Vídeo | YOLO, OpenCV, MediaPipe, Action Recognition |

## Fluxo típico multimodal

```python
Vídeo → extrair frames + áudio
         ├─ frames → visão (detecção, OCR, facial)
         └─ áudio → transcrição (Whisper) → texto → NLP
                            ↓
              resultado combinado (JSON estruturado)
```

## Aplicações reais

- Monitoramento de câmeras (segurança)
- Legendagem automática de vídeos
- Moderar conteúdo (texto + imagem + vídeo)
- Análise de reuniões (transcrição + sentimentos + tópicos)

## Mini-projeto: triagem de transcrições (call center)

Você recebeu as transcrições das 4 últimas chamadas do suporte. O objetivo: **achar o tema dominante** — o problema que aparece em mais chamadas — para priorizar o fix. Este é o pipeline de texto mais usado em produção: transcrição → limpeza → contagem → decisão.

<Exercise
  lessonSlug="analise-video-audio"
  codeKey="mp4-audio"
  title="Mini-projeto — temas das transcrições"
  expectedOutput="cartao: 3x"
  hint="Rode o código: o Counter agrega os termos-chave das 4 transcrições. O tema mais citado aparece primeiro — e é o que o time de produto deve atacar."
/>

**O que observar:** "cartao" aparece 3x — o problema é o cartão (bloqueio, cobrança duplicada, uso não autorizado). Com apenas 4 transcrições e uma contagem, você achou o gargalo. Em produção, o mesmo padrão escala para milhares de chamadas com TF-IDF + clustering de tópicos.

## Próximos passos

Na próxima lição: **AWS Textract** — o serviço que extrai texto, tabelas e formulários de documentos digitalizados, indo além do OCR tradicional.
